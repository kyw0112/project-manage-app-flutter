import 'package:flutter/cupertino.dart';

class ProjectGoto extends StatelessWidget {
  const ProjectGoto({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("바로가기");
  }
}
